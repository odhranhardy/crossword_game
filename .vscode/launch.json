{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Kivy App",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/.venv/bin/python"
        }
    ]
}